import React, {ReactElement} from 'react';

const NotAuthorized = (): ReactElement => {
    return (
        <div>
            <p>NotAuthorized</p>
        </div>
    );
};

export default NotAuthorized;
